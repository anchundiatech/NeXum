/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const EMPRESA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  180, 198, 160, 163, 183, 172, 76, 137,
]);

export function getEmpresaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EMPRESA_DISCRIMINATOR);
}

export type Empresa = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  nombre: string;
  pais: string;
  balance: bigint;
  totalEnviado: bigint;
  totalRecibido: bigint;
  activa: boolean;
};

export type EmpresaArgs = {
  owner: Address;
  nombre: string;
  pais: string;
  balance: number | bigint;
  totalEnviado: number | bigint;
  totalRecibido: number | bigint;
  activa: boolean;
};

/** Gets the encoder for {@link EmpresaArgs} account data. */
export function getEmpresaEncoder(): Encoder<EmpresaArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["pais", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["balance", getU64Encoder()],
      ["totalEnviado", getU64Encoder()],
      ["totalRecibido", getU64Encoder()],
      ["activa", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: EMPRESA_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Empresa} account data. */
export function getEmpresaDecoder(): Decoder<Empresa> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["pais", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["balance", getU64Decoder()],
    ["totalEnviado", getU64Decoder()],
    ["totalRecibido", getU64Decoder()],
    ["activa", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link Empresa} account data. */
export function getEmpresaCodec(): Codec<EmpresaArgs, Empresa> {
  return combineCodec(getEmpresaEncoder(), getEmpresaDecoder());
}

export function decodeEmpresa<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Empresa, TAddress>;
export function decodeEmpresa<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Empresa, TAddress>;
export function decodeEmpresa<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Empresa, TAddress> | MaybeAccount<Empresa, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getEmpresaDecoder(),
  );
}

export async function fetchEmpresa<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Empresa, TAddress>> {
  const maybeAccount = await fetchMaybeEmpresa(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeEmpresa<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Empresa, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeEmpresa(maybeAccount);
}

export async function fetchAllEmpresa(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Empresa>[]> {
  const maybeAccounts = await fetchAllMaybeEmpresa(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeEmpresa(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Empresa>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeEmpresa(maybeAccount));
}
