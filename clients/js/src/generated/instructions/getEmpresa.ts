/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NEXUM_PROGRAM_ADDRESS } from "../programs";

export const GET_EMPRESA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  109, 112, 67, 152, 107, 247, 165, 67,
]);

export function getGetEmpresaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_EMPRESA_DISCRIMINATOR);
}

export type GetEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountEmpresa extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEmpresa extends string
        ? ReadonlyAccount<TAccountEmpresa>
        : TAccountEmpresa,
      ...TRemainingAccounts,
    ]
  >;

export type GetEmpresaInstructionData = { discriminator: ReadonlyUint8Array };

export type GetEmpresaInstructionDataArgs = {};

export function getGetEmpresaInstructionDataEncoder(): FixedSizeEncoder<GetEmpresaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_EMPRESA_DISCRIMINATOR }),
  );
}

export function getGetEmpresaInstructionDataDecoder(): FixedSizeDecoder<GetEmpresaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetEmpresaInstructionDataCodec(): FixedSizeCodec<
  GetEmpresaInstructionDataArgs,
  GetEmpresaInstructionData
> {
  return combineCodec(
    getGetEmpresaInstructionDataEncoder(),
    getGetEmpresaInstructionDataDecoder(),
  );
}

export type GetEmpresaInput<TAccountEmpresa extends string = string> = {
  empresa: Address<TAccountEmpresa>;
};

export function getGetEmpresaInstruction<
  TAccountEmpresa extends string,
  TProgramAddress extends Address = typeof NEXUM_PROGRAM_ADDRESS,
>(
  input: GetEmpresaInput<TAccountEmpresa>,
  config?: { programAddress?: TProgramAddress },
): GetEmpresaInstruction<TProgramAddress, TAccountEmpresa> {
  // Program address.
  const programAddress = config?.programAddress ?? NEXUM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    empresa: { value: input.empresa ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("empresa", accounts.empresa)],
    data: getGetEmpresaInstructionDataEncoder().encode({}),
    programAddress,
  } as GetEmpresaInstruction<TProgramAddress, TAccountEmpresa>);
}

export type ParsedGetEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    empresa: TAccountMetas[0];
  };
  data: GetEmpresaInstructionData;
};

export function parseGetEmpresaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetEmpresaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { empresa: getNextAccount() },
    data: getGetEmpresaInstructionDataDecoder().decode(instruction.data),
  };
}
