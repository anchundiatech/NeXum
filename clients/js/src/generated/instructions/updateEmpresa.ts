/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NEXUM_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_EMPRESA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  18, 242, 134, 167, 47, 236, 233, 63,
]);

export function getUpdateEmpresaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_EMPRESA_DISCRIMINATOR,
  );
}

export type UpdateEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountEmpresa extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEmpresa extends string
        ? WritableAccount<TAccountEmpresa>
        : TAccountEmpresa,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateEmpresaInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: Option<string>;
  pais: Option<string>;
};

export type UpdateEmpresaInstructionDataArgs = {
  nombre: OptionOrNullable<string>;
  pais: OptionOrNullable<string>;
};

export function getUpdateEmpresaInstructionDataEncoder(): Encoder<UpdateEmpresaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "nombre",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      [
        "pais",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_EMPRESA_DISCRIMINATOR }),
  );
}

export function getUpdateEmpresaInstructionDataDecoder(): Decoder<UpdateEmpresaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "nombre",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    [
      "pais",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getUpdateEmpresaInstructionDataCodec(): Codec<
  UpdateEmpresaInstructionDataArgs,
  UpdateEmpresaInstructionData
> {
  return combineCodec(
    getUpdateEmpresaInstructionDataEncoder(),
    getUpdateEmpresaInstructionDataDecoder(),
  );
}

export type UpdateEmpresaInput<
  TAccountEmpresa extends string = string,
  TAccountOwner extends string = string,
> = {
  empresa: Address<TAccountEmpresa>;
  owner: TransactionSigner<TAccountOwner>;
  nombre: UpdateEmpresaInstructionDataArgs["nombre"];
  pais: UpdateEmpresaInstructionDataArgs["pais"];
};

export function getUpdateEmpresaInstruction<
  TAccountEmpresa extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof NEXUM_PROGRAM_ADDRESS,
>(
  input: UpdateEmpresaInput<TAccountEmpresa, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): UpdateEmpresaInstruction<TProgramAddress, TAccountEmpresa, TAccountOwner> {
  // Program address.
  const programAddress = config?.programAddress ?? NEXUM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    empresa: { value: input.empresa ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("empresa", accounts.empresa),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getUpdateEmpresaInstructionDataEncoder().encode(
      args as UpdateEmpresaInstructionDataArgs,
    ),
    programAddress,
  } as UpdateEmpresaInstruction<
    TProgramAddress,
    TAccountEmpresa,
    TAccountOwner
  >);
}

export type ParsedUpdateEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    empresa: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: UpdateEmpresaInstructionData;
};

export function parseUpdateEmpresaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateEmpresaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { empresa: getNextAccount(), owner: getNextAccount() },
    data: getUpdateEmpresaInstructionDataDecoder().decode(instruction.data),
  };
}
