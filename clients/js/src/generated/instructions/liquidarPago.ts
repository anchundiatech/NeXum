/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NEXUM_PROGRAM_ADDRESS } from "../programs";

export const LIQUIDAR_PAGO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  107, 53, 210, 189, 236, 89, 228, 207,
]);

export function getLiquidarPagoDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LIQUIDAR_PAGO_DISCRIMINATOR,
  );
}

export type LiquidarPagoInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountProtocolo extends string | AccountMeta<string> = string,
  TAccountEmpresaOrigen extends string | AccountMeta<string> = string,
  TAccountEmpresaDestino extends string | AccountMeta<string> = string,
  TAccountTransaccion extends string | AccountMeta<string> = string,
  TAccountPagador extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProtocolo extends string
        ? WritableAccount<TAccountProtocolo>
        : TAccountProtocolo,
      TAccountEmpresaOrigen extends string
        ? WritableAccount<TAccountEmpresaOrigen>
        : TAccountEmpresaOrigen,
      TAccountEmpresaDestino extends string
        ? WritableAccount<TAccountEmpresaDestino>
        : TAccountEmpresaDestino,
      TAccountTransaccion extends string
        ? WritableAccount<TAccountTransaccion>
        : TAccountTransaccion,
      TAccountPagador extends string
        ? WritableSignerAccount<TAccountPagador> &
            AccountSignerMeta<TAccountPagador>
        : TAccountPagador,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type LiquidarPagoInstructionData = {
  discriminator: ReadonlyUint8Array;
  monto: bigint;
};

export type LiquidarPagoInstructionDataArgs = { monto: number | bigint };

export function getLiquidarPagoInstructionDataEncoder(): FixedSizeEncoder<LiquidarPagoInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["monto", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: LIQUIDAR_PAGO_DISCRIMINATOR }),
  );
}

export function getLiquidarPagoInstructionDataDecoder(): FixedSizeDecoder<LiquidarPagoInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["monto", getU64Decoder()],
  ]);
}

export function getLiquidarPagoInstructionDataCodec(): FixedSizeCodec<
  LiquidarPagoInstructionDataArgs,
  LiquidarPagoInstructionData
> {
  return combineCodec(
    getLiquidarPagoInstructionDataEncoder(),
    getLiquidarPagoInstructionDataDecoder(),
  );
}

export type LiquidarPagoInput<
  TAccountProtocolo extends string = string,
  TAccountEmpresaOrigen extends string = string,
  TAccountEmpresaDestino extends string = string,
  TAccountTransaccion extends string = string,
  TAccountPagador extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  protocolo: Address<TAccountProtocolo>;
  empresaOrigen: Address<TAccountEmpresaOrigen>;
  empresaDestino: Address<TAccountEmpresaDestino>;
  transaccion: Address<TAccountTransaccion>;
  pagador: TransactionSigner<TAccountPagador>;
  systemProgram?: Address<TAccountSystemProgram>;
  monto: LiquidarPagoInstructionDataArgs["monto"];
};

export function getLiquidarPagoInstruction<
  TAccountProtocolo extends string,
  TAccountEmpresaOrigen extends string,
  TAccountEmpresaDestino extends string,
  TAccountTransaccion extends string,
  TAccountPagador extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NEXUM_PROGRAM_ADDRESS,
>(
  input: LiquidarPagoInput<
    TAccountProtocolo,
    TAccountEmpresaOrigen,
    TAccountEmpresaDestino,
    TAccountTransaccion,
    TAccountPagador,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): LiquidarPagoInstruction<
  TProgramAddress,
  TAccountProtocolo,
  TAccountEmpresaOrigen,
  TAccountEmpresaDestino,
  TAccountTransaccion,
  TAccountPagador,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NEXUM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    protocolo: { value: input.protocolo ?? null, isWritable: true },
    empresaOrigen: { value: input.empresaOrigen ?? null, isWritable: true },
    empresaDestino: { value: input.empresaDestino ?? null, isWritable: true },
    transaccion: { value: input.transaccion ?? null, isWritable: true },
    pagador: { value: input.pagador ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("protocolo", accounts.protocolo),
      getAccountMeta("empresaOrigen", accounts.empresaOrigen),
      getAccountMeta("empresaDestino", accounts.empresaDestino),
      getAccountMeta("transaccion", accounts.transaccion),
      getAccountMeta("pagador", accounts.pagador),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getLiquidarPagoInstructionDataEncoder().encode(
      args as LiquidarPagoInstructionDataArgs,
    ),
    programAddress,
  } as LiquidarPagoInstruction<
    TProgramAddress,
    TAccountProtocolo,
    TAccountEmpresaOrigen,
    TAccountEmpresaDestino,
    TAccountTransaccion,
    TAccountPagador,
    TAccountSystemProgram
  >);
}

export type ParsedLiquidarPagoInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    protocolo: TAccountMetas[0];
    empresaOrigen: TAccountMetas[1];
    empresaDestino: TAccountMetas[2];
    transaccion: TAccountMetas[3];
    pagador: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: LiquidarPagoInstructionData;
};

export function parseLiquidarPagoInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedLiquidarPagoInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      protocolo: getNextAccount(),
      empresaOrigen: getNextAccount(),
      empresaDestino: getNextAccount(),
      transaccion: getNextAccount(),
      pagador: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getLiquidarPagoInstructionDataDecoder().decode(instruction.data),
  };
}
