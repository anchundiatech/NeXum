/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NEXUM_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_EMPRESA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([199, 216, 68, 141, 228, 45, 54, 198]);

export function getRegisterEmpresaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_EMPRESA_DISCRIMINATOR,
  );
}

export type RegisterEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountEmpresa extends string | AccountMeta<string> = string,
  TAccountProtocolo extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEmpresa extends string
        ? WritableAccount<TAccountEmpresa>
        : TAccountEmpresa,
      TAccountProtocolo extends string
        ? WritableAccount<TAccountProtocolo>
        : TAccountProtocolo,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterEmpresaInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: string;
  pais: string;
};

export type RegisterEmpresaInstructionDataArgs = {
  nombre: string;
  pais: string;
};

export function getRegisterEmpresaInstructionDataEncoder(): Encoder<RegisterEmpresaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["pais", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_EMPRESA_DISCRIMINATOR }),
  );
}

export function getRegisterEmpresaInstructionDataDecoder(): Decoder<RegisterEmpresaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["pais", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getRegisterEmpresaInstructionDataCodec(): Codec<
  RegisterEmpresaInstructionDataArgs,
  RegisterEmpresaInstructionData
> {
  return combineCodec(
    getRegisterEmpresaInstructionDataEncoder(),
    getRegisterEmpresaInstructionDataDecoder(),
  );
}

export type RegisterEmpresaInput<
  TAccountEmpresa extends string = string,
  TAccountProtocolo extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  empresa: Address<TAccountEmpresa>;
  protocolo: Address<TAccountProtocolo>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  nombre: RegisterEmpresaInstructionDataArgs["nombre"];
  pais: RegisterEmpresaInstructionDataArgs["pais"];
};

export function getRegisterEmpresaInstruction<
  TAccountEmpresa extends string,
  TAccountProtocolo extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NEXUM_PROGRAM_ADDRESS,
>(
  input: RegisterEmpresaInput<
    TAccountEmpresa,
    TAccountProtocolo,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterEmpresaInstruction<
  TProgramAddress,
  TAccountEmpresa,
  TAccountProtocolo,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NEXUM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    empresa: { value: input.empresa ?? null, isWritable: true },
    protocolo: { value: input.protocolo ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("empresa", accounts.empresa),
      getAccountMeta("protocolo", accounts.protocolo),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterEmpresaInstructionDataEncoder().encode(
      args as RegisterEmpresaInstructionDataArgs,
    ),
    programAddress,
  } as RegisterEmpresaInstruction<
    TProgramAddress,
    TAccountEmpresa,
    TAccountProtocolo,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterEmpresaInstruction<
  TProgram extends string = typeof NEXUM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    empresa: TAccountMetas[0];
    protocolo: TAccountMetas[1];
    owner: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterEmpresaInstructionData;
};

export function parseRegisterEmpresaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterEmpresaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      empresa: getNextAccount(),
      protocolo: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterEmpresaInstructionDataDecoder().decode(instruction.data),
  };
}
